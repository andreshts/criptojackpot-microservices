# Lottery API - Base Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lottery-api
  namespace: cryptojackpot
  labels:
    app: lottery-api
    tier: api
spec:
  selector:
    matchLabels:
      app: lottery-api
  template:
    metadata:
      labels:
        app: lottery-api
        tier: api
    spec:
      containers:
        - name: lottery-api
          image: cryptojackpot/lottery-api
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: LOTTERY_DB_CONNECTION
            - name: JwtSettings__SecretKey
              valueFrom:
                secretKeyRef:
                  name: jwt-secrets
                  key: JWT_SECRET_KEY
            - name: JwtSettings__Issuer
              valueFrom:
                secretKeyRef:
                  name: jwt-secrets
                  key: JWT_ISSUER
            - name: JwtSettings__Audience
              valueFrom:
                secretKeyRef:
                  name: jwt-secrets
                  key: JWT_AUDIENCE
            - name: Kafka__BootstrapServers
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: ConnectionStrings__RedisConnection
              valueFrom:
                secretKeyRef:
                  name: redis-secrets
                  key: REDIS_CONNECTION_STRING
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"  