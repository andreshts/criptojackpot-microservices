# Kustomization for QA secrets
# NOTE: These are placeholders - actual values should be managed via:
# - Sealed Secrets
# - External Secrets Operator
# - Or manual kubectl create secret
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - jwt-secrets.yaml
  - postgres-secrets.yaml
  - kafka-secrets.yaml
  - mongodb-secrets.yaml
  - spaces-secrets.yaml
  - brevo-secrets.yaml
  - redis-secrets.yaml
