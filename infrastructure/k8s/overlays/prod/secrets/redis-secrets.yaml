# Secrets for PRODUCTION - Redis (Upstash)
apiVersion: v1
kind: Secret
metadata:
  name: redis-secrets
  namespace: cryptojackpot
type: Opaque
stringData:
  REDIS_CONNECTION_STRING: "REPLACE_WITH_UPSTASH_REDIS_ENDPOINT:6379,password=REPLACE_WITH_UPSTASH_REDIS_PASSWORD,ssl=True,abortConnect=False"

