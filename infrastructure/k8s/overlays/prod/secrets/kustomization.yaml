# Kustomization for Production secrets
# IMPORTANT: These are placeholders - actual values MUST be managed via:
# - Sealed Secrets (recommended)
# - External Secrets Operator
# - Manual kubectl create secret (not recommended for prod)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - jwt-secrets.yaml
  - postgres-secrets.yaml
  - kafka-secrets.yaml
  - mongodb-secrets.yaml
  - spaces-secrets.yaml
  - brevo-secrets.yaml
  - redis-secrets.yaml
