apiVersion: apps/v1
kind: Deployment
metadata:
  name: winner-api
  namespace: cryptojackpot
  labels:
    app: winner-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: winner-api
  template:
    metadata:
      labels:
        app: winner-api
    spec:
      containers:
        - name: winner-api
          image: registry.digitalocean.com/cryptojackpot/winner-api:v1.0.0
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: WINNER_DB_CONNECTION
            - name: Kafka__BootstrapServers
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_BOOTSTRAP_SERVERS
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: winner-api
  namespace: cryptojackpot
spec:
  selector:
    app: winner-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

