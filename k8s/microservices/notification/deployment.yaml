apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-api
  namespace: cryptojackpot
  labels:
    app: notification-api
    tier: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-api
  template:
    metadata:
      labels:
        app: notification-api
        tier: api
    spec:
      containers:
        - name: notification-api
          image: registry.digitalocean.com/cryptojackpot/notification-api:v1.0.0
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: NOTIFICATION_DB_CONNECTION
            - name: Kafka__BootstrapServers
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: Kafka__SaslUsername
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_SASL_USERNAME
            - name: Kafka__SaslPassword
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_SASL_PASSWORD
            - name: Kafka__SaslMechanism
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_SASL_MECHANISM
            - name: Kafka__SecurityProtocol
              valueFrom:
                secretKeyRef:
                  name: kafka-secrets
                  key: KAFKA_SECURITY_PROTOCOL
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: cryptojackpot
spec:
  selector:
    app: notification-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

